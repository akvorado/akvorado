apiVersion: v1
kind: Service
metadata:
  name: orchestrator
  namespace: {{ .Values.environment }}-{{ .Values.namespace }}
  labels:
    app: orchestrator
spec:
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: orchestrator