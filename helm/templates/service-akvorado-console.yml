apiVersion: v1
kind: Service
metadata:
  name: console
  namespace: {{ .Values.environment }}-{{ .Values.namespace }}
  labels:
    app: console
spec:
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: console