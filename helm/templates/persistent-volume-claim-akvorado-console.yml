apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: akvorado-console-pvc
  namespace: {{ .Values.environment }}-{{ .Values.namespace }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      {{- if and (.Values.volumes)  (.Values.volumes.akvorado_console) (.Values.volumes.akvorado_console.storageSize) }}
      storage: {{ .Values.volumes.akvorado_console.storageSize }}
      {{- else }}
      storage: 5Gi
      {{- end }}
  storageClassName: "standard"
  volumeMode: Filesystem